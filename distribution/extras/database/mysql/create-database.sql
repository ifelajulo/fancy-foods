CREATE TABLE FOOD (
	name VARCHAR(255),
	price DECIMAL,
	quantity INT,
	PRIMARY KEY (name)
);
CREATE TABLE CUSTOMER (
	name VARCHAR(255),
	creditLimit DECIMAL,
	balance DECIMAL,
	PRIMARY KEY (NAME)
);
